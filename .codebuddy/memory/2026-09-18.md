# 2026-09-18

## cognee 调研（他山之石）
- 完成 topoteretes/cognee v1.5.4 调研，报告：`docs/cognee-调研与借鉴分析.md`（本地克隆 D:\repos\cognee，HEAD c0d18c80e）。
- **2026-09-19 grill 复核推翻初稿结论**：8 个借鉴点全部证伪、0 采纳。6 条本仓已有等价实现（secret_redact 双路径脱敏、usage_heat 封顶/冷惩罚/采纳加权、pending-status≈水位线、tool_digest 错误链≈LIVE 快路径、file_lock+noop≈抢锁放弃、submit 单次多产出），2 条语义不同不采纳（raw 优先级标记、novelty 限定同类节点集）。报告 §三/§五已按证伪结论改写，cognee 价值定位为「独立收敛互证」。
- 不借：图库+向量底座、无确认闸门自动加权、truth subspace 质心、全局上下文索引分桶摘要。

## 蒸馏积压处理（他山之石）
- 后台 worker 的 submit 被并行流程抢先（noop），其 3 条提取（ADR-0011 落地闭环 / CJK 分词坑 / 子集降级规则）经复核后以 ingest_note(status=draft) 补录，暂存 .distill 文件已清理。
- 5 条草稿（3 补录 + 2 并行流程）已全部 confirm 为 stable（3 条 strong 带 ADR-0011 证据，2 条 weak），git 已自动推送。#1 与既有 graphiti 结论笔记是落地闭环关系，后续可考虑 merge。


- 报告落盘 `docs/llm-wiki-compiler-调研与借鉴分析.md`，基线 v1.3.0 / HEAD c2c85bb（2026-09-17），克隆在 D:\repos\llm-wiki-compiler。
- 结论：10 候选 → 0 采纳 / 3 deferred / 7 excluded。真差异主线是「lint 规则布尔检查 → 量化评分+阈值门禁+历史 delta」（deferred 待消费场景）；freshness 四态、原子写、profile-as-data、OKF、review 扣留本仓均已有等价实现。
- 三条工程习惯吸收（不立项）：apply 时重断言底线、诚实边界写进代码注释、已知范围限制就地标注。

## 主动沉淀与任务关联机制确认（产品维护会话）

- `add_task_memory` 的 task_id 必填，记忆天然任务级；`ingest_note` 有可选 `task_id` 参数写入 note frontmatter，由 `get_task_context` 的 related_notes 和 `query_wiki(task_id=...)` 消费（registry.py:1039/1230）；`delete_task` 删任务目录但不删盖了 task_id 的笔记（registry.py:2794）。主动沉淀协议两条路径均可关联任务。

## frontmatter 解析 bug 修复（产品维护）

- 根因：`_read_frontmatter`（note_consolidation.py:124）用 `text.find("---", 3)` 找结束标记，frontmatter 值内含 `---`（如笔记文件名 `commit---amend`）即截断 YAML、解析失败 → lint 误报 SKILL.md 缺 name/status/source_refs。
- 修复：结束标记按行匹配 `^---\s*$`（逐行累计偏移）。验证：解析 7 keys/6 refs 正常，54 passed，skill_lint 3 error→0。
- 遗留：全仓约 30 处同款 `find("---", 3)` 模式（wiki_lint/source_ingest/doctrine/page_manifest/note_writer/close_session/note_query/note_lifecycle），待收敛成共享 helper；MCP server 长驻进程需重启才生效。

## 知识飞轮全链路（产品维护）

- 笔记聚合：60 条 → 8 场景块更新 + 1 新建「竞品调研与借鉴方法」，57 条退役（reject_note reason="consolidated into <场景>"），3 条 deferred（skill-creator、团队化冲突治理×2，待实施后独立成场景）。
- Doctrine 刷新：新增竞品调研 SOP + 「文档站宣传口径/代码事实口径」判断逻辑；char_cap 1200 压线通过（多次超限重试，教训：先数好字数再提交）；confirm_note 后 stable，计数器归零。
- lint：3 error 均为既有 skills/windows-dev-env frontmatter 缺字段，与本次无关。

## 压缩摘要上限优化（产品维护）

- `_COMPACTION_SUMMARY_MAX_CHARS` 2048→4096（task_manager.py:198）；超限报错给出超出字数（over by N）；zh/en compact_instruction 补超限重试指引；设计文档 §5.2 同步；测试 2049→4097 + over by 1 断言。68 passed。
- 背景：用户质疑「70 字限制」实为 2048 字符上限的误读（报错差额被误读）；实测 34 条早期记忆压到 2048 偏紧。
- 任务记忆压缩流程：prepare 返回 entries_to_compress + existing_summary + instruction → submit 校验 ≤4096 → 原子重写 memories.md + append 归档。

## AGENTS.md 主动沉淀协议优化 + 文本块精简（产品维护）

- 用户反馈主动沉淀未触发（Q&A 轮只写了 IDE 工作记忆未写任务记忆）。三缺口：判据不含机制澄清类轮次；宿主强指令产生已沉淀错觉；无 per-turn 自查。
- 实施：`prompts.py` `_active_settle_section()` 判据扩面（+澄清/纠偏关键认知）+ 每轮收尾自查 + 双通道独立声明；`_TASK_MEMORY_AGENTS_SECTION` 精简；`locales/zh.yaml`/`en.yaml` `artifacts.agents_md.main` 大幅精简（保留测试断言短语）。
- 重新生成 AGENTS.md 三块：`write_agents_md()`（LLM Wiki 块）+ `upsert_agents_section()`（TASK-MEMORY 块）+ `upsert_active_settle_protocol(p, ide='codebuddy', active_settle=True)`（ACTIVE-SETTLE 块）。AGENTS.md 约 200 行 → 165 行。
- 测试：122 + 109 passed。`Agent skills`/`Team memory fusion` 两段为手写内容不在管理块内，未动。

## Code-review 修复完成（Standards 9 项 + Spec 8 项）

- **Spec#6 序号错位（最重）**：`supersede_memory` 改为按注入侧同规则编号——own+legacy 合并时间序、仅无持久 ID 条目编 `#e` 序号（复用新共享函数 `store.entry_display_ids`）；索引标记归属（`(own_idx, entry)` 元组），重复文本不会误判文件归属；legacy 条目 ref 显式报错拒绝。
- **Spec#7 坏 ref 留孤儿**：`supersede_memory` 加 `dry_run` 参数，`handle_add_task_memory` 先 dry_run 验证 ref 再追加新条目；坏 ref 返回 `{"error":..., "supersedes":...}` 且不落盘。
- **Spec#1 检索侧缺失**：`search_memories` 过滤 superseded 条目出打分语料，结果带 `superseded` 字段标注。
- **Spec#2 off-by-one**：`_write_check` 窗口计数含本条（`len+1 > 5`），第 6 条写入即带 hint。
- **Spec#3 边界**：`_compaction_level` 改 `>=`（30/35 条即 yellow/orange），移除规格外 `_COMPACTION_KEEP` 门槛，参数化消除重复分支。
- **Spec#8 摘要稀释**：摘要逐份独立比对 + 剥离 `## 早期记忆（摘要）` 标题行（标题稀释与条目头同类问题）。
- **Standards**：`_render_hot` O(n²) `.index()` 改索引直传；`_split_compaction` 返回 `(compress, keep)` 二元组按 `id()` 判成员，消除相等误删；registry 描述去 "append-only"；CONTEXT.md 错字（单行行键→单行键）、术语对齐（每会话→每压缩窗口）、Key decisions 补 0009/0010。U+FFFD 乱码为 subagent 误报（实测无）。
- **设计文档**：D3 补实施防御（剥头/短文本豁免）、D5 补逐份独立比对、验收链 2 改「不追加」、批次表「双匹配校验」→「ref 校验」。
- **测试**：更新 `test_supersede_bad_ref_errors_not_silent`（新语义：error 且不落盘）；新增 3 个：`test_search_skips_superseded_and_annotates`、`test_write_check_window_counts_incoming_entry`、`test_write_check_compares_summaries_individually`。全量 1159 通过、2 跳过。
- 踩坑：列表推导式条件里引用未绑定变量 `s`（`if s.startswith` 在 `for s` 之前）→ 用 `for s in [p[1]]` 尾部绑定修正。

## 补蒸馏完成：7 条积压全部清空（distill-worker-0918）

- Mode C 流程：prepare（7 条清单）→ 逐条 read_file → submit（大载荷走 `.distill-*.json` 文件侧通道）→ 复验 prepare 返回空。
- 产出：新建草稿笔记 10 条、merge 进既有笔记 3 条、update 替换旧决策笔记 1 条（2026-09-16 阻塞式 → 异步式）、空提取 3 条（hook 空信封噪声，no_knowledge）；memories_written=0（通道互斥）。
- 踩坑：内联 submit 大载荷超限报错，改 distilled_file 成功；一次调用超时但服务端实际已完成（merge 已落盘、暂存文件已删）——**submit 超时后先查服务端状态再决定是否重试，避免双写**。
- 冲突裁决经验：候选笔记先 read 再判 store/merge/update/skip；新旧决策对撞用 update（正文替换+推翻说明）；互补知识用 merge（追加章节）。

## 三项收尾完成（ADR-0010 + D2 表述修正 + 验收链 9 测试）

- **ADR-0010 落盘**：`docs/adr/0010-task-memory-channel-mutual-exclusion.md`（通道互斥：task_id 过滤的补蒸馏路径固定 skip_memories，无 opt-out；非任务过滤路径仍走 ADR-0008 原料标记）。全部悬空引用「ADR-0009 通道互斥」→「ADR-0010」，涉及：`distill_conversation.py`（3 处）、`prompts.py`（2 处）、`AGENTS.md`、`codewiki/agents/distill-worker.md`、`distill-worker.claude.md`、`.qoder/.trae` 镜像、`tests/test_task_manager.py`。
- **D2 表述修正**：设计文档与 ADR-0009 的「ID+时间戳双匹配」改为实际实现的单 ref 匹配（`store.py:1294`：`entry_id(e)==ref or #e{ordinal}==ref`），附实施简化说明；验收链 2 同步改为「坏 ref 显式报错」。
- **验收链 9 测试**：`tests/test_distill_cleanup.py` 新增 `test_prepare_next_carries_revision_hint`，断言 prepare 响应 `next` 含 "OVERTURNS"/"AMENDS"/"cite the note file name"。
- 踩坑：`AGENTS.md` 的 `replace_in_file` 报成功但内容未变（疑似 IDE 侧还原），改用 PowerShell `[IO.File]::WriteAllText` 直接改写成功——**编辑 AGENTS.md 后必须用 Select-String 复核**。
- 测试：`test_distill_cleanup.py` + `test_task_manager.py` + `test_distill_p1.py` 共 75 项全通过。

## ADR-0009 实施完成（supersede + 写入检查 + 分级 + 修订提示）

- **store.py**：`_ENTRY_ID_RE`/`_SUPERSEDED_RE`（**必须 re.M**，标记在条目第二行——踩坑：忘加 re.M 导致 `entry_is_superseded` 永远 False）；`entry_id`/`entry_is_superseded`/`new_entry_id`（4 位 base36，防撞）；`format_memory_entry` 加 `entry_id_` 参数；`append_memories` 加 `entry_id_override`；`supersede_memory`（`locked_rmw` 契约：transform 返回 None=中止，错误须以异常穿出——踩坑：错误当字符串返回被当成新文件内容）。
- **task_manager.py**：`_entry_display_ids`（惰性序号按时间序，全量 hot 层编号，过滤/截断不漂移）+ `_annotate_entry`（注入时给无持久 ID 条目补序号，不回写文件）；`_load_memories_layered` 返回 5 元组（加 `compaction_level`），过滤 superseded；`_write_check`（difflib ratio>0.85 拒绝，**比对剥离条目头**——踩坑：含时间戳头会稀释 ratio；**短文本 <20 字符豁免**——踩坑：「记忆0」vs「记忆1」ratio 0.9 误拒）；`handle_add_task_memory` 加 `supersedes` 参数（supersede 时跳过去重）、返回 `entry_id`/`hot_entries`/`hint`；`_compaction_level` 黄橙红（75%/87.5%/100% 内插，`compaction_due` 保留为 red 别名）；`_split_compaction`（superseded 优先摘出）。
- **registry.py**：`add_task_memory` schema 加 `supersedes`；**prompts.py + AGENTS.md**：主动沉淀协议块补写入标准（只记改变下一步行动的、推翻传 supersedes、近重复会被拒）。
- **distill_conversation.py**：prepare 提示补「推翻/修订既有笔记时在新笔记中引用并说明」。
- **测试**：新增 7 个验收链测试（supersede 标记与过滤、坏 ref 显式报错、近重复拒绝、不同内容放行、supersede 跳过检查、分级四档、压缩优先级）；更新 2 个与 ADR-0009 冲突的旧断言（legacy 注入字节相同→文件字节相同+注入带序号；纯时间戳头→带持久 ID）。全量 1154 通过，1 个 Windows 文件锁 flaky（`test_atomic_write_thread_safe`，单独跑通过，与本次无关）。
- 发现既有文档债：代码 6 处引用「ADR-0009 通道互斥」但 `docs/adr/` 无此文件（编号被我占用）——留待后续补 ADR 或改引用。

## 任务记忆退役与写入检查：grill 定案 + ADR-0009 + 设计文档

- 两轮 grill（Q1-Q11 全按推荐）定案：本期做 supersede 失效语义（惰性条目 ID + 单行 `> [superseded 日期 by #ID]` 标记 + 双匹配校验）、写入检查（difflib ratio>0.85 拒绝 + 压缩窗口软上限警告放行 + 返回 hot_entries 计数）、compaction_level 黄橙红分级（75%/87.5%/100% 内插）、蒸馏 prepare 修订提示；Handoff 快照立项独立排期；蒸馏事件流+整数 ID 映射砍掉。
- 落盘：`docs/adr/0009-task-memory-supersede-and-write-check.md`（append-only 放宽为 append+受控降状态）、`docs/任务记忆退役与写入检查设计方案.md`（D1-D11 + 验收链 9 条 + 四批次实施切分，约 3 人日）；CONTEXT.md 新增「退役（retired）」「写入检查（write check）」两词条。
- 关键事实：任务记忆条目无 ID（`task_manager.py:154` 时间戳头）；压缩是读取驱动（`_compaction_needed` `task_manager.py:204-213`，40 条/24KB）；写入路径不触发压缩；格式 Markdown（ADR-0001）。
- 横向格式对比结论：随业务仓 git 版本化存记忆全场只有 teamai-cli 与本仓；Markdown 存记忆是少数派（OpenViking/teamai-cli/本仓），其余 SQLite/向量库——本仓零 embedding 单机场景下 Markdown+BM25 自洽，ADR-0001 判断被支持。
- 下一步：按设计文档批次①（supersede）开始实施，排期对齐「统一知识存储层」任务。

## graphiti 调研（他山之石）

- 本地克隆 `D:\repos\graphiti`（getzep/graphiti，HEAD de8eb5b 2026-09-17），核心在 `graphiti_core/`。
- **工作机制**：`add_episode` 流水线 = 取最近 N 条 episode 做上下文 → LLM 抽取实体/关系（Pydantic schema 约束）→ 三级去重（精确名匹配 → MinHash/LSH 模糊匹配（熵门控防短名误判）→ LLM 兜底裁决）→ 边级矛盾检测（新事实 vs 旧事实，LLM 判 contradicted → 设 `invalid_at`/`expired_at` 软失效，不删除）→ 属性 overlay 合并（LLM 漏抽字段保留旧值）→ 嵌入 → 落库。
- **检索**：BM25 + 余弦 + BFS 多路召回，RRF/MMR/cross-encoder/node-distance/episode-mentions 五种 reranker，16 个预置 recipe（`search_config_recipes.py`）。
- **社区**：label propagation 聚类 + 两两摘要归并生成社区节点。
- **值得借鉴**（对照 CodeWiki 现状）：① 确定性快路径 + LLM 兜底的分层去重（CodeWiki 冲突检测目前纯 LLM）；② 事实级时间戳软失效（valid_at/invalid_at/expired_at）比 CodeWiki 的 supersede 单行标记更细；③ 属性 overlay 合并防 LLM 漏抽清空旧值；④ 检索 recipe 预设化（CodeWiki 已有 mode 但无组合预设）；⑤ LLM 输出防御性校验（越界 ID 剔除、缺 ID 告警，流程不崩）。
- **不借鉴**：图数据库依赖（CodeWiki 零 embedding/单机 Markdown 自洽）、自动社区摘要（成本高且与确认闸门哲学冲突）。
- 调研报告已落盘 `docs/graphiti-调研与借鉴分析.md`；同会话确认了 9 条蒸馏草稿笔记（8 新建 + 2026-09-16 阻塞式笔记被异步决策替换后重新确认），其余 3 条合并进既有 stable 笔记无需再确认。

## ADR-0011 实施（graphiti 借鉴落地，归产品维护）

- **Round 1 论断纠错**：`_title_tokens` 按空白切分，中文标题整题单 token，「发版本」vs「发版本流程」Jaccard=0.0（不是 0.5）——中文标题从未进过弱冲突带，E 的正确修法是复用 `retrieval.tokenize`（jieba）而非加熵门控。
- **更严重发现**：`_parse_llm_notes` 解析失败返回 `[]` → `no_knowledge` → raw 被永久删除——LLM 输出烂 = 原料丢失。
- **已实施**（distill_conversation.py）：① `_parse_llm_notes` 返回 `(notes, parse_error)` 二元组，失败时 raw 保留 + `status=parse_failed` + 显式 `parse_error` 字段；② `_title_tokens` 复用 `codewiki.src.retrieval.tokenize`；③ 缺 title/content 的 note 条目剔除并记 `invalid_note`；④ smoke 测试断言更新 + 新增 `tests/test_distill_defensive.py`（6 用例）。
- **验证**：新测试 6/6 绿；全量 1165 passed, 2 skipped。
- ADR：`docs/adr/0011-distill-defensive-validation-and-cjk-title-tokens.md`。
- 不做：检索 recipe 预设化、退役时间维度（理由见 ADR）。

## ADR-0011 Round 2（grill 第二轮，实测驱动）

- **实测暴露新风险**：E 修复后「任务记忆压缩」vs「任务记忆压缩设计方案」= 0.75 直落强重复带自动 suppress（误杀）；「发版本」vs「发版本流程」= 0.5 进 hold 带（正确）。
- **Q5(c) 实施 + 修正**：`_is_title_subset` 子集降级——但全量测试抓到反例：「...删除 cache key」vs「...删除 key」= 0.857 是同义改写不该降级。最终规则：真子集且 sim ∈ [0.6, 0.8) 才降级（`_SUBSET_STRONG_CEILING = 0.8`），≥0.8 近全同与完全相同仍走快路径。
- **Q6(b) 实施**：`_mark_parse_failed` 把 parse_error 写进 raw frontmatter（status 保持 pending），下轮 worker 可见失败原因。
- **验证**：test_distill_defensive 10 用例 + test_ingest_conflict_advisory + test_task_manager 共 73 passed；全量首轮 2 failed 中 1 个是本改动引发（已修），1 个是 flaky（单独跑通过）。
- **教训**：改判定带阈值后必须用真实标题对实测分布，直觉的「子集=歧义」被 0.857 反例证伪。

## schema.yaml 补齐 aggregation 配置节

- `repowiki/schema.yaml` 的 `conventions:` 下新增 `aggregation:` 节（consolidation_threshold: 10 / doctrine_threshold: 50 / hint_interval: 5 / max_scenarios: 15），此前一直走 `aggregation_state.py:29-32` 的代码内置默认值。
- 配置由 `read_config()`（aggregation_state.py:88-109）每次调用实时读取 schema.yaml，改完无需重启 MCP server。已用 Python 直读验证 4 个参数生效。
- max_scenarios 是 L2 场景块硬上限，分级预警：黄 = max-3、橙 = max-1、红 = 满（note_consolidation.py:379-391）；每批最多新建 1 个场景块。

## max_scenarios 默认值 15 → 20

- `aggregation_state.py:32` `DEFAULT_MAX_SCENARIOS` 15→20；`repowiki/schema.yaml` aggregation.max_scenarios 同步 15→20。
- 测试 helper（test_consolidation_p2.py:33 / test_doctrine_p3.py:32）的 `max_scenes: int = 15` 是显式传参的测试夹具默认值，不依赖代码默认值，无需改。25 passed 验证无回归。
- skill_creator 的容量硬顶 12（类比 max_scenarios）是独立设计，未动。
