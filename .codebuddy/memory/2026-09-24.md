# 2026-09-24

## distill-worker 补蒸馏：任务「他山之石」积压清空
- 处理 1 条 raw（164K 字符/167 轮，friction 45），产出 5 条草稿笔记，memories 通道互斥跳过（memories_written=0）。
- 去重裁决：1 条 update——「UserPromptSubmit active-settle 薄触发已实施」原地替换 2026-09-20 的 deferred 方案笔记（该方案已实施并真机验证，旧状态作废）；其余 4 条 store/ingested。
- 笔记主题：ADR-0015 落地（AGENTS.md 托管块瘦身，改产品模板）、active-settle 薄触发实施、SubagentStart hook 不做（subagent 只报告沉淀原料）、Workspace Conventions 接入清单收敛为工具指针、hook 注册用 python -m 包内入口。
- skill_hint：python -m 包内入口笔记命令密度 4（阈值 3），可能值得编译成技能——需用户确认，不自动执行。

## codewiki-plus 自动升级方案讨论
- 用户问：如何让用户无感知地自动升级 codewiki-plus。
- 现状：PyPI 发布（v5.13.1），入口 `codewiki` CLI + MCP server，无任何版本检查/自升级逻辑。
- 推荐方案：启动时后台静默升级（lazy self-update）——启动时查 PyPI JSON API（24h 缓存、fail-open、尊重 CODEWIKI_NO_AUTOUPDATE），发现新版后派生分离的后台子进程，等父进程退出后再 `sys.executable -m pip install -U`（Windows 文件锁：长驻 MCP server 加载 tree-sitter 原生依赖，运行中升级必失败）。
- 挂载点：`codewiki/cli/main.py:main()`、`codewiki/mcp/server.py:main()`、可选 SessionStart hook。
- 关键边界：必须用 `sys.executable -m pip`（全局 codewiki.exe 与 venv 错位是已踩过的坑，见 repowiki 笔记 2026-09-18）；源码安装（CODEWIKI_HOME/editable）跳过；只升不降。
- 配套：`codewiki upgrade` 显式子命令 + version 输出提示新版。
- 状态：方案已给出，待用户确认是否实现。
- 用户质疑：wait-for-exit 要等到智能体/IDE 关闭，等待过久。回应要点：
  - 运行中进程本就无法热加载，立即升级也不会让当前会话生效；且升级会造成懒加载版本混跑，"等退出"是正确性要求而非仅文件锁妥协。
  - 改进：①锁预检双模式（探测 .pyd 可写则立即升级，有锁才 wait-for-exit）；②CLI/hook 等短命进程触发点秒级落盘，互补；③等待设超时（30-60min）后重做锁预检；④lock file 防并发 pip。
  - 要求会话内生效的唯一路径：版本化安装 + shim（rustup 模式），但改变 pip 分发方式，暂不推荐。

## 拷问式设计定案（grill-me，Q1–Q14 全部按推荐锁定）
- Q1 默认开启自动升级 + `CODEWIKI_NO_AUTOUPDATE` 退出开关 + 配置持久化 + version 输出留痕。
- Q2 只对纯 pip/uv venv 启用；pipx/uv tool/conda 检测到即跳过、提示手动命令。
- Q3 semver 闸门：patch+minor 自动升，major 只提示。
- Q4 信任 pip 原子性 + 启动时廉价健康自检（元数据版本 vs import 版本不一致则重装自愈）。
- Q5 探针实测完成（见下）。
- Q6 混合策略：纯 Python 改动 rename-aside 零等待；tree-sitter 系列原生依赖版本变动（比较 PyPI requires_dist）则退回 wait-for-exit。
- Q7 状态文件 `~/.codewiki/autoupdate.json`（last_check/last_version/last_result/pending_target）。
- Q8 互斥锁 `~/.codewiki/.autoupdate.lock`，O_CREAT|O_EXCL 原子创建，10 分钟超时抢占。
- Q9 `codewiki upgrade`（前台立即升级）+ `--check`（只查不装）；不做 --rollback。
- Q10 健康自检仅在 last_result=upgraded 后的首次启动跑。
- Q11 挂载点只挂 CLI main() + MCP server main()；hook 不挂（fail-open 纯采集定位，收益与 server 重叠）。
- Q12 全异步：主进程只读本地状态文件（微秒级），PyPI 查询+升级全在 detached 子进程，零阻塞。
- Q13 单元测试（mock PyPI/pip）+ 集成测试（file:/// 本地 index 装真 wheel，子进程持锁模拟运行中 server），CI 三平台。
- Q14 直接随下个 minor 版本全量开启，不灰度。
- 探针实测（Windows 11 + Py3.12 venv）：加载中 .pyd 写打开 FAIL / 重命名 OK / 删除 FAIL；DETACHED_PROCESS 子进程父退出后存活。已沉淀草稿笔记：notes/2026-09-24-windows-加载中的-pyd-可重命名不可写不可删rename-aside-自动升级技巧的实测依据.md（待 confirm_note）。
- 状态：设计共识达成，待用户最终确认后开始实现。

## 自动升级已实现（同日完成）
- 新增 `codewiki/utils/self_update.py`（核心模块：状态文件/环境闸门/semver 闸门/互斥锁/detached 子进程/健康自检/前台升级）+ `codewiki/utils/__init__.py`。
- 新增 `codewiki/cli/commands/upgrade.py`（`codewiki upgrade [--check]`），注册进 `codewiki/cli/main.py`。
- 挂载点两处：`codewiki/cli/main.py:main()` 与 `codewiki/mcp/server.py:main()`，各一行 `maybe_self_update()`（fail-open）。
- 测试 `tests/test_self_update.py`：26 个单元测试全过；ruff 全绿；全量回归 1179 passed。
- 探针笔记已 confirm_note 转正（human:wangbao）。
- 待办：集成测试（file:/// 本地 index 装真 wheel + 持锁子进程）尚未写（Q13 的集成部分）；随下个 minor 版本发布（Q14）。

## agentmemory 调研（他山之石）
- 克隆 D:\repos\agentmemory（rohitg00/agentmemory v0.9.29，~42K LOC/264 函数），报告落盘 repowiki/wiki/queries/agentmemory-调研.md。
- 定位：重运行时路线极致样本——常驻 iii-engine（三原语 worker/function/trigger）、hooks 全量自动采集、四层记忆巩固（working/episodic/semantic/procedural）+ 衰减驱逐、BM25+向量+图 RRF 检索。
- 三候选全部不立项：合成压缩→absorbed（与确认闸门冲突）；自动衰减驱逐→deferred（与「候选必有去向」Doctrine 冲突，等 lint_wiki low_adoption 数据）；slots→excluded（MEMORY.md+AGENTS.md+任务记忆三件套已覆盖）。
- 总体判断：全自动采集/巩固/驱逐路线 CodeWiki 明确不走，调研价值在确认架构边界。
- 任务记忆压缩：50 条超阈值（红色），已按 prepare 指令压缩 30 条为摘要，保留 20 条，原文归档 memories-archive/iamwangbao-163-com.md。

## README 专栏文章列表更新
- 新增第 12 篇公众号文章链接：[本地知识库团队化改造](https://mp.weixin.qq.com/s/Ebsk6CoLN5Wj7ejwF3ytfQ)（2026-09-21），追加在 README.md 中文「专栏文章」列表第 11 篇之后。
- 技巧：微信文章标题从页面 `og:title` meta 抓取，发布时间从 `publish_time` 时间戳（Unix 秒）换算。
