# 2026-09-17

## 记忆机制对照调研（提取质量/防臃肿/失效更新）

- 结论：本仓 notes 通道提取质量已是第一梯队（五纪律+priority 门+两阶段去重，`distill_congestion.py:59-121/614-736`）；**薄弱面在任务记忆通道**——直写追加、无去重、无质量门、无失效语义。
- 三关切对照：TAM（条数限流 10 条/次、L2 ≤15 场景红橙黄容量预警、每批最多新建 1、L3 ≤1200 字增量五策略）；powercontext（只记改变未来工作的信息、revise 修订优先于新增、Handoff disposition 快照）；mem0（事件流 ADD/UPDATE/DELETE、整数 ID 反幻觉、MD5 前置去重——自动无闸门执行已否决）；HL-MM supersedes_id 链（旧条目降状态不物理删）；OpenViking 字段级 merge_op。
- 建议 P0：①任务记忆 supersede 失效语义（add_task_memory 加 supersedes 参数或 void_task_memory，旧条目打标跳过注入）；②任务记忆写入质量门（协议块补「只记改变下一步行动的进展」+ 工程侧相似度去重 + 每会话软上限 5 条）。P1：Handoff 快照（powercontext 候选 1 落法已有）、蒸馏事件流+整数 ID 映射（带 confirm 闸门版）。P2：compaction_due 红橙黄分级、蒸馏 prompt 补「修订优先于新增」。
- 约束：不重提已否决方向——mem0 无闸门自动 UPDATE/DELETE（9-17 决策笔记）、半衰期衰减（C11）、自动冲突发现（ADR-0007）、向量检索。

## 任务管理 Hook 启用验证（install-hooks --active-settle on）

- 全局 `codewiki` CLI 入口失效（Python314 Scripts 下的 exe 报 `ModuleNotFoundError: No module named 'codewiki'`），改用仓库 venv：`d:\repos\CodeWiki-Plus\.venv\Scripts\python.exe -m codewiki.cli.main install-hooks --repo-path d:\repos\CodeWiki-Plus --active-settle on` 成功。
- 三个智能体（codebuddy/qoder/trae）接线幂等完成：hook 脚本与 distill-worker.md 强制覆盖拷贝，settings.json/hooks.json 均已 wired 无需改动，AGENTS.md 的 TEAM-MEMORY-TASK 与 CODEWIKI-ACTIVE-SETTLE 两标记块齐全（159-179 / 181-200 行）。
- 模拟事件验证：SessionEnd（正斜杠 cwd）落盘 `conv-测试.md`（source_session=verify-1）✓；SessionStart 注入任务关联 additionalContext ✓；TRAE Stop 无 transcript 不落盘（预期降级）✓。测试产物已清理。
- 坑：PowerShell 单引号字符串里 `d:\repos` 的 `\r` 会被 JSON 解析器当非法转义拒绝——模拟事件 cwd 必须用正斜杠。

## 补蒸馏通道互斥：固定只产经验笔记（ADR-0009）

- **语义**：补蒸馏路径（`distill_conversation` 带 `task_id` 过滤的 prepare/submit/Mode B）**固定** `skip_memories`——只提取经验笔记，任务记忆归主动沉淀通道（`add_task_memory` 直写）。无外部开关（用户明确要求删掉三态参数，固定语义）。
- **实现**：
  - `distill_conversation.py`：`_process_llm_output` 加 `skip_memories` 形参；handler 内 `skip_memories=bool(task_filter)`；prepare 响应带 `skip_memories: true` + `memories_note` 提前告知提取方；跳过不静默——响应带 `memories_skipped_reason: "skip_memories"`
  - `registry.py`：未加 schema 参数（固定语义，无入参）
  - 协议文案同步：hook（`task_session_start.py`）、`prompts.py`（AGENTS.md 协议块 + task-workflow）、根 `AGENTS.md`、`distill-worker.md`/`.claude.md`（提取时直接返回空 memories 数组）
  - 守门测试：`test_distill_task_scoped_skips_memories_by_default`（task 路径 0 记忆 + skip 原因；非 task 过滤路径双轨仍写入）
- **测试**：全量 1148 passed, 2 skipped
- **注意**：`.git/tmp-t*.txt` 测试输出临时文件清理命令被用户拒绝，可能残留

## 补蒸馏从阻塞式改回异步 + 蒸馏笔记补 sessionId 溯源

- **背景**：用户反馈阻塞式补蒸馏（先记忆后回答）与「用户在等回答」冲突，AGENTS.md 软约束遵循不稳定；要求改回异步（8-16 `ecc4fcf` 之前的形态），并让蒸馏产物可溯源到 sessionId。
- **根因分析**：AGENTS.md 不稳定触发不是内容量问题（200 行/15.7KB），而是软约束 + 指令冲突（阻塞等待 vs 立即回答）+ 长文档注意力稀释。异步化消除冲突本身。
- **改动**（全部文案层 + 一处溯源逻辑，无核心逻辑改动）：
  - `codewiki/hooks/task_session_start.py`：三处「阻塞式」→「异步」，主 Agent 不等 subagent、直接回答，自然停顿点拉取结果
  - `codewiki/mcp/prompts.py`：AGENTS.md 协议块 + task-workflow prompt 同步改异步
  - `codewiki/agents/distill-worker.md` + `.claude.md`：description 改异步
  - 根 `AGENTS.md` 协议块同步
  - 已装副本同步：`.codebuddy/.qoder/.trae` 下 hook + agent 定义（Copy-Item）
  - `tests/test_task_session_start.py:101`：断言改「异步」且「阻塞式」不存在
- **sessionId 溯源**：
  - `codewiki/mcp/tools/note_ingest.py`：ingest_note 新增 `source_session` 参数，写入笔记 metadata（与 task_id 并列）
  - `codewiki/mcp/tools/distill_conversation.py`：submit 路径从 raw frontmatter 读 `source_session` 传给 ingest（`_unquote_fm` 解析）
  - 新增守门测试 `tests/test_task_manager.py::test_distill_note_carries_source_session`
- **测试**：全量 1147 通过
- **SessionEnd Hook 结论**：主动沉淀（ACTIVE-SETTLE）+ 收尾轮保险采集可覆盖 SessionEnd Hook 的核心价值，用户可选择不装；hook 的优势是硬触发 vs AGENTS.md 软约束
- **注意**：`.git/tmp-*.txt` 调试临时文件清理命令被用户拒绝，可能残留
