# 2026-09-19

## L2 知识聚合（2026-09-20 执行）
- 52 条候选全部去向：38 条 absorbed（reject_note 退役）+ 6 条 disposition（4 excluded / 2 deferred）+ 8 条此前已聚合。
- 7 个既有场景 UPDATE + 1 个新场景 CREATE（技能提取与注入资产治理，吸收 9 条技能类笔记）；容量 11/20 绿色，计数器归零。
- 修复两个历史坏 frontmatter：场景块 source_notes 里含 `---` 的笔记文件名被 YAML 当闭合栅栏，导致 `type: Scenario` 解析丢失（发布与依赖治理、IDE-Hook 采集链路两块）——聚合 submit 校验拦截后手工修复。
- lint 0 error（53 warning 均为既有 stale_evidence/low_adoption）；close_session(force) 重建索引完成。
- 遗留：9 条 draft 文档待用户裁决是否 batch_set_status stable（含 6 条 mem0 调研笔记）。

## supermemory 调研（他山之石）
- 报告：`docs/supermemory-调研与借鉴分析.md`；克隆在 `D:\repos\supermemory`，基线 HEAD 57b430b（2026-09-18）。
- 开源仓=客户端/集成层（MCP server on Cloudflare DO、@supermemory/tools、SMFS、MemoryBench），核心引擎闭源。
- 12 候选：6 excluded（确认闸门/双层记忆/成组蒸馏等已有等价）、3 deferred（derives+isLatest、SMFS 哲学→Cli能力、MemoryBench→质量量化）、2 归任务参考（annotations 四档佐证、SKILL.md 范本→技能提取）。

## supermemory grill 复核（他山之石）
- 「declined 即 forgotten」本仓已等价且是三层设计：handler 跳过（note_query.py:1030/465/672）+ 索引降权压槽位（retrieval.py:485）+ 蒸馏去重豁免（distill_conversation.py:678）。
- 教训：判断权重是否死代码，先确认作用层与过滤时序（排序层降权在 handler 过滤前生效，不是死代码）。

## memos 调研（任务：他山之石）
- 克隆 D:\repos\memos（HEAD 7e3d3c6，v0.31.0-rc.2-16），报告落盘 `docs/memos-调研与借鉴分析.md`。
- 核心价值在 `server/mcp/`：OpenAPI 驱动工具目录、进程内回环执行、annotations 方法推导+覆盖表、structuredContent 规范化、错误不带 schema、任务级 eval（15 QA 钉种子数据）。
- 处置：absorbed 1（registry 加 annotations 参数）、deferred 3（structuredContent/错误形状同根；任务级 eval 并入质量量化主线）、excluded 6。
- 本仓现状核对：`codewiki/mcp/registry.py` ToolDef 无 annotations 字段；`server.py` call_tool 返回 TextContent，无 structuredContent/outputSchema。

## hindsight 调研（他山之石）
- 克隆 D:\repos\hindsight（HEAD 0a58d69，2026-09-19），报告落盘 `docs/hindsight-调研与借鉴分析.md`。
- vectorize.io 的重基建长期记忆系统（引擎+18 Agent 集成包），与本仓架构前提相反；可搬的只有集成层认知：索引抑制（注入块列清单→40 轮 0 次搜索，absorbed）、consolidation prompt 两条规则（reason 必带、NO COMPUTATION，absorbed 进 distill prompt）。
- deferred 2：page delta 刷新、4 臂 RRF 检索；其余 excluded（等价或前提不成立）。

## letta 调研（他山之石）
- `letta-ai/letta` main 分支只是 landing page，当前实现在 `letta-ai/letta-code`（TS 编码 agent）；克隆在 `D:\repos\letta-code`，报告落盘 `docs/letta-调研与借鉴分析.md`。
- 核心机制：`.letta` 目录 MemFS（Markdown+frontmatter）、memory 工具写入即 git commit 且 reason 必填、reflection 子代理默认每 25 step 在隔离 worktree 改记忆、mergePolicy auto/explicit 显式合并、失败压制自动反思、记忆与 Skill 同通道沉淀。
- **2026-09-20 grill 拷问定案（ADR-0013）**：落地两项——① ingest_note 加可选 reason 字段，stable 直写必填；② limits.py 收敛 task_manager 压缩阈值 + lint_wiki 加 threshold_drift 检查。排除四项：worktree 隔离、step-count 触发、失败压制（自我修正）、技能同通道（skill_hint 已有）。顺序 E 先 B 后。
- **E 项已实施**：limits.py + task_manager re-export + threshold_drift lint（首日抓到 registry.py 2048→4096 真实漂移）+ 6 测试用例，全量 1168+6 passed。B 项待实施。
- **B 项已实施（ADR-0013 全部落地）**：note_ingest.py reason 必填（stable）+ evidence 可选（复用 confirm_note 语义升 strong）；registry schema 同步；test_ingest_reason_evidence.py 7 用例；修 4 处既有裸 stable 调用；全量 1182 passed。

## 补蒸馏（他山之石，distill-worker-0919）
- 清空任务积压 2 条 raw（cognee 调研会话、memos 调研会话），产出 2 条 decision 草稿笔记待确认：cognee 调研处置（8 借鉴点全部证伪 0 采纳）、memos 调研处置（1 absorbed 挂起 / 3 deferred / 6 excluded）。
- 冲突候选 2 次均判 store（通用层 vs 项目结论层互补）。
- 踩坑：`distill_conversation` submit 内联 `distilled` 只接受以 conversation_id 为 key 的映射；裸 `{notes}` 形状 + conversation_id 参数会报 missing_result（裸形状仅适用于 distilled_file 文件通道）。

## hindsight 借鉴 grill 定案 + 落地（他山之石）
- grill 五题全按推荐：absorbed=必须有可指认落点（否则降级 deferred）；两条规则只落 `_DISTILL_SYSTEM`；reason+NO COMPUTATION 合并一条；deferred 不定触发信号；索引抑制不动代码。
- 已落地：`_DISTILL_SYSTEM` 第 6 条纪律「Reasoned and literal」（distill_conversation.py），test_distill_p1.py 补断言，全量 1169 passed；ADR-0012 落盘；decision 草稿补「落地」章节；调研报告处置表 #3 改 absorbed（已落地）。
- 待办：两条草稿笔记（索引抑制 pitfall、distill 规则 decision）仍待 confirm_note。
